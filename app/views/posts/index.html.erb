<div class="mb-8">
  <%= render "form" %>
</div>

<div id="posts" class="divide-y">
  <% @posts.each do |post| %>
    <%= render partial: "posts/#{post.post_type}", locals: { post: post } %>
  <% end %>
</div>

<div class="p-6" id="empty-posts"></div>
