<%= turbo_stream.update_all(".#{dom_id(@likeable, :likes)}") do %>
  <%= render "likes/likes", likeable: @likeable %>
<% end %>

<%= turbo_stream.update_all(".#{dom_id(@likeable, :likes_count)}", @likeable.likes.count) %>
